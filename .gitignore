.env
pgdata/
# TLS cert/key and config.yaml (holds plaintext userpass credentials once
# internal/hysteria.SyncUsers runs) — not for version control.
hysteria/*.pem
hysteria/*.key
hysteria/config.yaml

# Go
api/vpn-api
api/vpn-createadmin
*.exe
*.test
*.out
# *.test above targets compiled Go test binaries, not this committed,
# secrets-free test-environment file (see CLAUDE.md "Testing").
!/.env.test

# OS
.DS_Store
